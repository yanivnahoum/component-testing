/**
 * Write 2 spring test classes that access a postgres db, initialized with the script in src/main/resources/db/init.sql
 * They should use a singleton container, set to be reusable.
 * Each of the 2 spring test classes should have a single test method.
 * 1. UserDao.count(): already implemented
 * 2. UserDao.getName(int id): Get the name of user with id=1
 */
package com.att.training.ct.exercises;

